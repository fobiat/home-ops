---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
components:
  - ../../../../components/namespace-privileged
resources:
  - ./grafana-admin-credentials.sops.yaml
  - ./helmrelease.yaml
  - ./httproute.yaml
  # ./alertmanager-discord-webhook.sops.yaml: add once the real webhook is
  # encrypted (see alertmanager-discord-webhook.PLACEHOLDER.yaml in this dir).
