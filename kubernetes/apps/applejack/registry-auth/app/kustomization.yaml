---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: applejack
resources:
  - ./serviceaccount.yaml
  # ghcr-pull-secret (dockerconfigjson for ghcr.io/fobiat) doesn't exist yet.
  # Create it with sops the same way cairn's registry-auth secret was
  # created, then add ./ghcr-pull-secret.sops.yaml here.
