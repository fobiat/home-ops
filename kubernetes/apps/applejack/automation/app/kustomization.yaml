---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./imagerepository.yaml
  - ./imagepolicy.yaml
  - ./imageupdateautomation.yaml
  # This ImageRepository needs its own ghcr-pull-secret in flux-system (the
  # package is private). Create it with sops the same way cairn's was
  # created, then add ./ghcr-pull-secret.sops.yaml here.
