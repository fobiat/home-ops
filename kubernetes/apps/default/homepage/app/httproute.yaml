---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homepage
spec:
  parentRefs:
    - name: internal
      namespace: network
      sectionName: https
  hostnames:
    - home.lab.fobiat.dev
  rules:
    - backendRefs:
        - name: homepage
          port: 3000
