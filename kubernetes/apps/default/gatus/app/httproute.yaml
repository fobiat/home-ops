---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gatus
spec:
  parentRefs:
    - name: internal
      namespace: network
      sectionName: https
  hostnames:
    - status.lab.fobiat.dev
  rules:
    - backendRefs:
        - name: gatus
          port: 80
