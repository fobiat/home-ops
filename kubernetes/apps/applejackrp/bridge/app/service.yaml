---
apiVersion: v1
kind: Service
metadata:
  name: applejackrp-bridge
spec:
  selector:
    app.kubernetes.io/name: applejackrp-bridge
  ports:
    - name: http
      port: 8080
      targetPort: http
