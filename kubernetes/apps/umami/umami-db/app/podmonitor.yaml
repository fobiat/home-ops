---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: umami-db
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: umami-db
  podMetricsEndpoints:
    - port: metrics
