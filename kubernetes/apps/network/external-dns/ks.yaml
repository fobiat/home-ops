---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: network-external-dns
  namespace: flux-system
spec:
  targetNamespace: network
  dependsOn:
    - name: network-gateway
  commonMetadata:
    labels:
      app.kubernetes.io/name: network-external-dns
  path: ./kubernetes/apps/network/external-dns/app
  prune: true
  wait: true
  interval: 1h
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
